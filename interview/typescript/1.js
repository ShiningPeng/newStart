const getUserInfo1 = function(user){
  return `name:${user.name},age:${user.age}`
}

// getUserInfo({name:'koala',age:20})
// getUserInfo({name:'koala',height:1.68})
// getUserInfo({name:'koala'})
// getUserInfo()