import React, { useState, useEffect, useRef, useCallback } from 'react';

const Search = (props) => {
    return (
        <>
            search
        </>
    )
}

export default React.memo(Search);